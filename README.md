# Global-Convection-Mapping
Code to implement the G-LDFF convection mapping algorithm. The code requires the Intel c-compiler, and it requires that the paths for access to the SuperDARN observations be created.
